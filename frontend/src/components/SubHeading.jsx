import React from 'react'

export default function SubHeading({ label }) {
    return <div>
        <p className="font-semibold text-zinc-500 text-center mb-4 whitespace-pre-line">{label}</p>
    </div>

}
